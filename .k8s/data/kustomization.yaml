apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: laravel

labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/part-of: laravel
      app.kubernetes.io/managed-by: kustomize

resources:
  - sealedsecret.yaml
  - postgres.yaml
  - redis.yaml
